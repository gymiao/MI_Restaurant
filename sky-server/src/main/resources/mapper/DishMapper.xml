<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id" parameterType="com.sky.entity.Dish">
        insert into dish (name, category_id, price, image, description, status)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="com.sky.entity.Dish">
        update dish
        <set>
            <if test="name!=null">
                name = #{name},
            </if>
            <if test="categoryId!=null">
                category_id = #{categoryId},
            </if>
            <if test="price!=null">
                price = #{price},
            </if>
            <if test="image!=null">
                image = #{image},
            </if>
            <if test="description!=null">
                description = #{description},
            </if>
            <if test="status!=null">
                status = #{status}
            </if>
        </set>
        where id=#{id}
    </update>

    <delete id="deleteBatch">
        delete from dish where id in
        <foreach collection="ids" open="(" close=")" separator="," item="itm">
            #{itm}
        </foreach>
    </delete>

    <select id="pageQuery" resultType="com.sky.vo.DishVO" parameterType="com.sky.dto.DishPageQueryDTO">
        select d.*, c.name as category_name from dish d left outer join category c on d.category_id = c.id
        <where>
            <if test="categoryId!=null">
                and d.category_id = #{categoryId}
            </if>
            <if test="name!=null">
                and d.name like concat('%', #{name}, '%')
            </if>
            <if test="status!=null">
                and d.status = #{status}
            </if>
        </where>
        order by d.create_time desc
    </select>


</mapper>
